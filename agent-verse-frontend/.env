# Backend base URL the API client uses (src/lib/api/client.ts reads VITE_API_BASE_URL).
# Dev backend runs on :8001 (matches vite.config proxy + .env.local).
VITE_API_BASE_URL=http://localhost:8001
VITE_WS_URL=ws://localhost:8001
