export function ReflexionEvidenceView({ evidence }: { evidence: Array<Record<string, unknown>> }) {
  return <section aria-labelledby="reflexion-heading"><h3 id="reflexion-heading" className="font-semibold">Reflexion evidence</h3><ul className="mt-3 space-y-2">{evidence.map((item, index) => { const confidence = typeof item.confidence === 'number' ? `${Math.round(item.confidence * 100)}%` : 'unscored'; return <li key={String(item.memory_id ?? index)} className="rounded-md border p-3 text-sm"><div className="flex justify-between gap-2"><span>{String(item.applicability ?? 'General lesson')}</span><span className="font-mono">{confidence}</span></div><p className="mt-1 text-xs text-muted-foreground">{String(item.provenance ?? 'No provenance')} · {item.quarantined ? 'Quarantined' : 'Eligible'}</p></li>; })}</ul></section>;
}
